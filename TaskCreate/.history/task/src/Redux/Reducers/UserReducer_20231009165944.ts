import { ActionType, User } from "./Types";

const user: User = {
    id: 0,
    name: '',
    email: '',
    password: '',
    passwordSubmit: ''
};

export const UserReducer = (state = user, action: ActionType): ActionType => {
    switch (action.type) {
        case :
            
            break;
    
        default:
            break;
    }
}