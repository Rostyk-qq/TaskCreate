import { User } from "./Types";

const user: User = {
    id: 0,
    name: '',
    email: '',
    password: '',
    passwordSubmit: ''
};

export const UserReducer = (user, action): user => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}