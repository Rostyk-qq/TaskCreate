import { User } from "./Types";

const user: User = {
    id: 0,
    name: '',
    email: '',
    password: '',
    passwordSubmit: ''
};

export const UserReducer = (user, action: A): user => {
    switch (action.type) {
        case :
            
            break;
    
        default:
            break;
    }
}